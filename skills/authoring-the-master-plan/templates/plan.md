# Goal
<one specific, testable outcome — what this project is FOR, in 1–2 sentences>

# Required Output
- **Deliverable:** <the artifact(s) produced — app, CLI, API, report, model, …>
- **Format / interface:** <how it is consumed — command, endpoint, file, UI, …>
- **Acceptance criteria:**
    - <criterion 1 — objectively checkable by a test or command>
    - <criterion 2>
    - <criterion 3>
- **Out of scope:** <what this explicitly does NOT deliver>

# Steps
1. step0_setup — <scaffold: .gitignore, .env(.example), config, docs, conductor skeleton>
2. step1_<name> — <one deliverable slice> → step1_<name>/design.md
3. step2_<name> — <…> → step2_<name>/design.md
4. …
